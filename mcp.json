{
  "name": "health-monitor-mcp",
  "version": "1.1.0",
  "mcpName": "io.github.oaslananka/health-monitor-mcp",
  "description": "Monitor MCP servers with health checks, uptime history, response times, alerts, and operational reports",
  "transport": "stdio",
  "command": "npx",
  "args": ["-y", "health-monitor-mcp"],
  "env": {
    "HEALTH_MONITOR_DB": {
      "description": "Custom path for the SQLite database (optional)",
      "required": false,
      "default": "~/.mcp-health-monitor/health.db"
    },
    "HEALTH_MONITOR_AUTO_CHECK": {
      "description": "Set to '1' to enable background scheduled checks",
      "required": false,
      "default": "0"
    },
    "HEALTH_MONITOR_RETENTION_DAYS": {
      "description": "Number of days to retain health check history",
      "required": false,
      "default": "30"
    },
    "HEALTH_MONITOR_MAX_CONCURRENCY": {
      "description": "Maximum concurrent scheduled and interactive server checks",
      "required": false,
      "default": "5"
    }
  },
  "tools": [
    "register_server",
    "check_server",
    "check_all",
    "get_uptime",
    "get_dashboard",
    "get_report",
    "list_servers",
    "unregister_server",
    "set_alert",
    "get_monitor_stats"
  ]
}
