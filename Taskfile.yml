version: '3'

tasks:
  setup:
    cmds:
      - corepack enable
      - corepack prepare pnpm@11.14.0 --activate
      - pnpm install --frozen-lockfile
  ci:
    cmds:
      - pnpm run ci
  build:
    cmds:
      - pnpm run build
  test:
    cmds:
      - pnpm test
