trigger: none
pr: none

pool:
  vmImage: ubuntu-24.04

steps:
  - checkout: self
    clean: true
  - script: |
      echo "Publishing is managed by GitHub release-please and the guarded npm-production environment."
      echo "This Azure pipeline is intentionally non-publishing."
    displayName: Publish disabled
